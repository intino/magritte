package magritte;

public class AcceptedStashReader {

//    @Test
//    public void should_read_a_polymorphic_stash_into_repository() throws Exception {
//        Stash stash = createRadiatorStash();
//        Node node = read(stash).into(new Capsule(null));
//        assertThat(node.types().size(), is(3));
//        assertThat(node.toString(), is("Case[Instance:Radiator]"));
//        assertThat(node.is(Tag.Case), is(true));
//        assertThat("Node is instance of Thermal", check(node).isInstanceOf("Thermal"));
//        assertThat("Node is instance of Electrical", check(node).isInstanceOf("Electrical"));
//        assertThat("Node is instance of Radiator", check(node).isInstanceOf("Radiator"));
//        assertThat("Node is instance of Appliance", check(node).isInstanceOf("Appliance"));
//        assertThat("Node is instance of Device", check(node).isInstanceOf("Device"));
//    }
//

}
