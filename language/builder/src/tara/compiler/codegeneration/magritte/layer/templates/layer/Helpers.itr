def value(date) trigger(variableType)
	java.time.LocalDateTime
end

def value(time) trigger(variableType)
	java.time.LocalTime
end

def value(Double) trigger(variableType)
	double
end

def value(INTEGER) trigger(variableType)
	int
end

def value(OBJECT) trigger(variableType)
	Object
end


def value(RESOURCE) trigger(variableType)
	java.net.URL
end

def value(integer) trigger(variableTypeList)
	Integer
end

def value(date) trigger(variableTypeList)
	java.time.LocalDateTime
end

def value(time) trigger(variableTypeList)
	java.time.LocalTime
end

def value(double) trigger(variableTypeList)
	Double
end

def value(OBJECT) trigger(variableTypeList)
	Object
end

def value(boolean) trigger(variableTypeList)
	Boolean
end

def value(string) trigger(variableTypeList)
	String
end

def value(resource) trigger(variableTypeList)
	java.net.URL
end

def value(type) trigger(variableTypeList)
	tara.magritte.Concept
end


def value(type) trigger(variableType)
	tara.magritte.Concept
end

def value(String) trigger(variableType)
	String
end

def value(boolean) trigger(variableType)
	boolean
end

def trigger(quoted)
	"$value"
end

def type(nativerule) trigger(interfaceClass)
	$interfaceClass+javaValidName
end

def type(wordrule) trigger(externalWordClass)
	$externalWordClass+javaValidName
end

def type(customrule) trigger(name)
	$aClass+javaValidName
end