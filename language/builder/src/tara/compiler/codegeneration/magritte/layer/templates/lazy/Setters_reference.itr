def type(variable & reference) !type(final) !type(target) !type(concept) !type(inherited) !type(overriden) !type(multiple) trigger(setter)
	public void $name+firstLowerCase+javaValidWord($type+variableType value) {
		this.$name+firtLowercase+javaValidWord = new tara.magritte.Reference(value.node());
	}
end