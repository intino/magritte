def type(Variable & reference & owner) !type(target) !type(concept) type(multiple) !type(inherited) !type(overriden) trigger(getter)
	public List<$type+variableType> $name+firstLowerCase+javaValidWord() {
		return new tara.magritte.utils.ReferenceList<>($name+javaValidWord+firtLowercase, $type+variableType.class);
	}

	public List<$type+reference> $name+firstLowerCase+javaValidWord(java.util.function.Predicate<$type+reference> predicate) {
		return $name+firstLowerCase+javaValidWord().stream().filter(predicate).collect(java.util.stream.Collectors.toList());
	}
end

def type(Variable & reference & owner) !type(target) !type(concept) !type(inherited) !type(overriden) trigger(getter)
	public $type+variableType $name+firstLowerCase+javaValidWord() {
		return $name+javaValidWord+firtLowercase.instance() == null ? null : $name+firstLowerCase+javaValidWord.instance().as($type+reference.class);
	}
end
