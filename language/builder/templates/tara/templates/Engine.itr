def type(engine)
	package $generatedLanguage+lowercase;

	import tara.magritte.Model;

	public class $generatedLanguage~Engine extends $generatedLanguage+lowercase.ModelWrapper implements tara.magritte.Engine {

		public $generatedLanguage~Engine(Model model) {
			super(model);
			_model.messageProvider().register("$generatedLanguage");
		}

		public void init() {
			// Insert init code here
		}

		public void execute() {
			// Insert execute code here
		}
	}
end