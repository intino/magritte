def type(domain)
	package $generatedLanguage+lowercase;

	import tara.magritte.Model;

	public class $generatedLanguage~Domain extends $generatedLanguage+lowercase.ModelWrapper implements tara.magritte.Domain {

		public $generatedLanguage~Domain(Model model) {
			super(model);
			_model.messageProvider().register("$generatedLanguage");
			// Insert code here
		}
	}
end