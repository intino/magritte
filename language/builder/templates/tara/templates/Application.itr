def type(launcher)
	import tara.magritte.Model;

	public class Application {

		public static void main(String$[$] args){
			Model model = Model.init($language+lowerCase.$language+firstUpperCase~Domain.class, $metaLanguage+lowerCase.$metaLanguage+firstUpperCase~Engine.class);
			model.engine().execute();
		}
	}
end