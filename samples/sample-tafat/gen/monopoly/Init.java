package monopoly;

import tara.magritte.Morph;
import tara.magritte.Node;

public class Init extends Square {

    public Init(Node node) {
        super(node);
    }

    public Init(Morph morph, Node node) {
        super(morph, node);
    }
}
