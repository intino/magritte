#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")box ${PROJECT_NAME}.${MODULE_NAME}.${PACKAGE_NAME}.${NAME}
#else
box ${PROJECT_NAME}.${MODULE_NAME}.${NAME}#end
#if (${PARENT_MODULE_NAME} && ${PARENT_MODULE_NAME} != "")use ${PROJECT_NAME}.${PARENT_MODULE_NAME} as metamodel#end


# ${DATE}. Documentation of the ${TYPE}
${TYPE} ${NAME}
