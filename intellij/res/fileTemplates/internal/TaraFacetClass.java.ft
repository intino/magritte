#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end

import magritte.schema.Node;
import magritte.schema.Variable;

public class ${NAME} extends ${PARENT} {

	public Electrical(Node node){
		super(node);
	}
}
