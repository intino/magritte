#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end

import ${PROJECT_NAME.toLowerCase()}.intentions.${FACET_PLURAL.toLowerCase()}.${TYPE}${FACET}Intention;
import magritte.morphs.*;

public class ${NAME}${FACET} extends ${NAME} implements ${TYPE}${FACET}Intention {

}
