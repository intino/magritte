package magritte;

import magritte.handlers.RandomNameGenerator;
import org.junit.Test;

public class AcceptedNameGenerator {
    @Test
    public void should_generate_a_legible_name() throws Exception {
        for (int i = 0; i < 0; i++) {
            System.out.println(new RandomNameGenerator().generate());
        }
    }
}
