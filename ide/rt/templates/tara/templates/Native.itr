def type(parameter)
	public static class $name+javaValidName_$uid implements [$language+lowercase.natives.]$contract+firstUpperCase, NativeCode {
		$nativeContainer $$;

		@Override
		$signature {
			$body
		}

		@Override
		public void set(Morph context) {
			$$ = ($nativeContainer) context;
		}

		@Override
		public Class<? extends Morph> $$Class() {
			return $nativeContainer.class;
		}
	}
end