#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end

import tara.lang.model.Rule;

public class ${NAME} implements Rule<${level}>{

	@Override
	public boolean accept(${level} value){
		return false; //TODO
	}
}
