def type(Box)
    package magritte.store;

    public class $name+firstUppercase extends magritte.handlers.Box.Dsl {
        public static final magritte.handlers.Box box = new $name+firstUppercase();

        protected magritte.handlers.Box$[] includes() {
            return new magritte.handlers.Box$[]{ $namebox...[,$NL]
            };
        }
    }
end
