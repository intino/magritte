def type(Box)
package magritte.store;

import magritte.handlers.Box;

public class $name+firstUppercase extends Box.Dsl {
    public static final Box box = new $name+firstUppercase();

    protected Box$[] includes() {
        return new Box$[]{ $namebox...[,$NL]
        };
    }
}
end
