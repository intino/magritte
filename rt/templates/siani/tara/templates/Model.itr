def type(scene)
	package $name+LowerCase;

	public class $name+FirstCamelCase extends magritte.wraps.Model {

	    public $name+FirstCamelCase(magritte.wraps.Morph morph) {
	        super(morph);
	    }

	    public $name+FirstCamelCase(magritte.Graph graph) {
			super(graph);
		}

		$root...[$NL$NL]
	}
end

def type(root) !type(single) trigger(root)
	public magritte.Set<$qn+reference> $name+FirstLowerCase+plural() {
        return _get("$name+FirstLowerCase+plural", $qn+reference.class);
    }

    public $qn+reference $name+FirstLowerCase(int index) {
        return $name+FirstLowerCase+plural().get(index);
    }
end

def type(root) type(single) trigger(root)
	public $qn+reference $name+FirstLowerCase() {
        return _get("$name+FirstLowerCase", $qn+reference.class).get(0);
    }
end