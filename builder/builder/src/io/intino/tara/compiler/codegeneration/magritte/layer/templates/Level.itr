def type(wrapper)
	package $workingPackage+lowercase;

	import io.intino.tara.magritte.Graph;

	public class $generatedLanguage+javaValidName+FirstUpperCase extends $workingPackage+lowercase.GraphWrapper {

		public $generatedLanguage+javaValidName(Graph graph) {
			super(graph);
		}
	}
end

